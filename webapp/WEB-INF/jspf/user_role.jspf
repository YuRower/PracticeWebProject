<%@ include file="/WEB-INF/jspf/directive/taglib.jspf"%>
<c:if test="${ empty user}">
	<c:choose>
		<c:when test="${role.name == 'admin' }">
			<c:redirect url="/front_controller?command=user_list" />
		</c:when>
		<c:when test="${role.name == 'user'}">
			<c:redirect url="/front_controller?command=user_list" />
		</c:when>
		<c:when test="${role.name == 'guest'}">
			<c:redirect url="/front_controller?command=user_list" />
		</c:when>
	</c:choose>
</c:if>